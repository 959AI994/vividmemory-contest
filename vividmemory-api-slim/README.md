# vividmemory-api-slim (contest subset)

Core FastAPI memory engine used by the contest adapter.

Runtime entrypoint: `vividmemory-api` (`vividmemory_api.main:main`)

Default listen port: `8888`

Health: `GET /health`
